name: Distribution Upgrade

on:
  workflow_dispatch:
    inputs:
      host:
        description: Inventory hostname to target
        required: true
        type: choice
        options:
          - gravelines
          - strasbourg
          - frankfurt
      confirm:
        description: Execute the upgrade (unchecked = check only)
        type: boolean
        default: false

concurrency:
  group: ${{ inputs.host }}
  cancel-in-progress: false

jobs:
  upgrade:
    name: Run distribution upgrade
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Ansible
        run: pip install ansible

      - name: Install Ansible collections
        run: ansible-galaxy collection install -r requirements.yml

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy
          chmod 600 ~/.ssh/deploy
          yq '.host_key' inventory/host_vars/${{ inputs.host }}.yml >> ~/.ssh/known_hosts

      - name: Write vault password
        run: |
          printf '%s' "${{ secrets.ANSIBLE_VAULT_PASSWORD }}" > /tmp/vault-pass
          chmod 600 /tmp/vault-pass

      - name: Run upgrade playbook
        run: |
          ansible-playbook upgrade.yml --limit "${{ inputs.host }}" -e confirm=${{ inputs.confirm }} --vault-password-file /tmp/vault-pass
