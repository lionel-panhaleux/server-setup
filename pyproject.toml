[project]
name = "server-setup"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["ansible>=13"]

[dependency-groups]
dev = [
    "ansible-lint>=26.2.0",
    "molecule>=25",
    "molecule-plugins[docker]>=23",
]
