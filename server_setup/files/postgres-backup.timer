[Unit]
Description=Scheduled pg_dump backup of every database in the cluster

[Timer]
OnCalendar=daily
Persistent=true
RandomizedDelaySec=30m
Unit=postgres-backup.service

[Install]
WantedBy=timers.target
