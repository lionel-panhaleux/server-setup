---
galaxy_info:
  author: lpanhaleux
  description: Create a postgres database + owner role, with backup and restore task files.
  license: MIT
  min_ansible_version: "2.14"
dependencies: []
# Requires the `community.postgresql` collection on the controller.
